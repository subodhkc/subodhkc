name: Scheduled IndexNow + Google Indexing Ping

on:
  schedule:
    # Every Monday at 09:00 UTC (04:00 CST)
    - cron: "0 9 * * 1"
  workflow_dispatch:

jobs:
  ping-search-engines:
    runs-on: ubuntu-latest
    env:
      GCP_SERVICE_ACCOUNT_JSON: ${{ secrets.GCP_SERVICE_ACCOUNT_JSON }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Python dependencies for Google API
        run: pip install cryptography

      - name: Fetch sitemap and extract all URLs
        id: sitemap
        run: |
          SITEMAP=$(curl -s "https://subodhkc.com/sitemap.xml")
          if [ -z "$SITEMAP" ]; then
            echo "::warning::Could not fetch sitemap, aborting"
            exit 1
          fi

          URLS=$(echo "$SITEMAP" | grep -oP '(?<=<loc>)[^<]+' | sort -u | python3 -c "
          import sys, json
          urls = [line.strip() for line in sys.stdin if line.strip()]
          print(json.dumps(urls))
          ")

          URL_COUNT=$(echo "$URLS" | python3 -c 'import sys,json; print(len(json.load(sys.stdin)))')
          echo "Found $URL_COUNT URLs in sitemap"
          echo "urls=$URLS" >> $GITHUB_OUTPUT
          echo "count=$URL_COUNT" >> $GITHUB_OUTPUT

      - name: Ping IndexNow (Bing, Yandex, Seznam, Naver)
        run: |
          URLS='${{ steps.sitemap.outputs.urls }}'
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" \
            -X POST "https://api.indexnow.org/indexnow" \
            -H "Content-Type: application/json" \
            -d "{\"host\": \"subodhkc.com\", \"key\": \"subodhkcindexnowkey2026\", \"keyLocation\": \"https://subodhkc.com/subodhkcindexnowkey2026.txt\", \"urlList\": $URLS}")
          echo "IndexNow response: $RESPONSE"
          if [ "$RESPONSE" = "200" ] || [ "$RESPONSE" = "202" ]; then
            echo "IndexNow ping successful — ${{ steps.sitemap.outputs.count }} URLs submitted"
          else
            echo "::warning::IndexNow returned $RESPONSE (non-fatal)"
          fi

      - name: Submit URLs to Google Indexing API
        if: ${{ env.GCP_SERVICE_ACCOUNT_JSON != '' }}
        run: |
          # Write the service account JSON to a temp file
          echo "$GCP_SERVICE_ACCOUNT_JSON" > /tmp/gcp-service-account.json

          # Get OAuth2 access token
          ACCESS_TOKEN=$(python3 -c "
          import json, urllib.request, urllib.parse, base64

          with open('/tmp/gcp-service-account.json') as f:
              key = json.load(f)

          # Create JWT
          import time
          header = {'alg': 'RS256', 'typ': 'JWT'}
          payload = {
              'iss': key['client_email'],
              'scope': 'https://www.googleapis.com/auth/indexing',
              'aud': 'https://oauth2.googleapis.com/token',
              'exp': int(time.time()) + 3600,
              'iat': int(time.time())
          }

          def b64encode(data):
              return base64.urlsafe_b64encode(data).rstrip(b'=').decode()

          import hashlib
          header_b64 = b64encode(json.dumps(header, separators=(',', ':')).encode())
          payload_b64 = b64encode(json.dumps(payload, separators=(',', ':')).encode())

          # Sign with RSA
          from cryptography.hazmat.primitives import hashes, serialization
          from cryptography.hazmat.primitives.asymmetric import padding
          private_key = serialization.load_pem_private_key(
              key['private_key'].encode(),
              password=None
          )
          signature = private_key.sign(
              f'{header_b64}.{payload_b64}'.encode(),
              padding.PKCS1v15(),
              hashes.SHA256()
          )
          jwt = f'{header_b64}.{payload_b64}.{b64encode(signature)}'

          # Exchange JWT for access token
          data = urllib.parse.urlencode({
              'grant_type': 'urn:ietf:params:oauth:grant-type:jwt-bearer',
              'assertion': jwt
          }).encode()
          req = urllib.request.Request('https://oauth2.googleapis.com/token', data=data)
          resp = urllib.request.urlopen(req)
          token = json.loads(resp.read())['access_token']
          print(token)
          ")

          if [ -z "$ACCESS_TOKEN" ]; then
            echo "::warning::Could not get Google access token"
            exit 0
          fi

          echo "Got Google OAuth2 access token"

          # Submit each URL to Google Indexing API (max 200/day, 3s delay to avoid 429)
          URLS='${{ steps.sitemap.outputs.urls }}'
          echo "$URLS" | python3 -c "
          import json, sys, urllib.request, time, urllib.error

          token = '$ACCESS_TOKEN'
          urls = json.load(sys.stdin)
          success = 0
          fail = 0
          rate_limited = False

          for url in urls[:200]:  # Daily quota is 200
              if rate_limited:
                  print(f'  SKIP: {url} — rate limited, stopping to preserve quota')
                  fail += 1
                  continue
              try:
                  data = json.dumps({'url': url, 'type': 'URL_UPDATED'}).encode()
                  req = urllib.request.Request(
                      'https://indexing.googleapis.com/v3/urlNotifications:publish',
                      data=data,
                      headers={
                          'Content-Type': 'application/json',
                          'Authorization': f'Bearer {token}'
                      }
                  )
                  resp = urllib.request.urlopen(req)
                  success += 1
                  print(f'  OK: {url}')
                  time.sleep(3)  # Rate limit: ~20 req/min to avoid 429
              except urllib.error.HTTPError as e:
                  if e.code == 429:
                      print(f'  429 RATE LIMITED: {url} — stopping submissions')
                      rate_limited = True
                      fail += 1
                  else:
                      print(f'  FAIL: {url} — HTTP {e.code}: {e.reason}')
                      fail += 1
                  time.sleep(3)
              except Exception as e:
                  print(f'  FAIL: {url} — {e}')
                  fail += 1
                  time.sleep(3)

          print(f'Google Indexing API: {success} submitted, {fail} failed')
          if rate_limited:
              print('::warning::Google Indexing API rate limited (429). Daily quota exhausted. Remaining URLs will be retried next run.')
          if fail > 0 and not rate_limited:
              print(f'::error::{fail} URL(s) failed to submit to Google Indexing API')
              sys.exit(1)
          "

      - name: Submit sitemap to Google Search Console
        if: ${{ env.GCP_SERVICE_ACCOUNT_JSON != '' }}
        run: |
          echo "$GCP_SERVICE_ACCOUNT_JSON" > /tmp/gcp-service-account.json

          ACCESS_TOKEN=$(python3 -c "
          import json, urllib.request, urllib.parse, base64, time
          from cryptography.hazmat.primitives import hashes, serialization
          from cryptography.hazmat.primitives.asymmetric import padding

          with open('/tmp/gcp-service-account.json') as f:
              key = json.load(f)

          header = {'alg': 'RS256', 'typ': 'JWT'}
          payload = {
              'iss': key['client_email'],
              'scope': 'https://www.googleapis.com/auth/webmasters',
              'aud': 'https://oauth2.googleapis.com/token',
              'exp': int(time.time()) + 3600,
              'iat': int(time.time())
          }

          def b64encode(data):
              return base64.urlsafe_b64encode(data).rstrip(b'=').decode()

          header_b64 = b64encode(json.dumps(header, separators=(',', ':')).encode())
          payload_b64 = b64encode(json.dumps(payload, separators=(',', ':')).encode())

          private_key = serialization.load_pem_private_key(key['private_key'].encode(), password=None)
          signature = private_key.sign(f'{header_b64}.{payload_b64}'.encode(), padding.PKCS1v15(), hashes.SHA256())
          jwt = f'{header_b64}.{payload_b64}.{b64encode(signature)}'

          data = urllib.parse.urlencode({'grant_type': 'urn:ietf:params:oauth:grant-type:jwt-bearer', 'assertion': jwt}).encode()
          req = urllib.request.Request('https://oauth2.googleapis.com/token', data=data)
          resp = urllib.request.urlopen(req)
          print(json.loads(resp.read())['access_token'])
          ")

          if [ -z "$ACCESS_TOKEN" ]; then
            echo "::warning::Could not get Google access token for sitemap submission"
            exit 0
          fi

          # Submit sitemap via GSC API (try both sc-domain and https%3A%2F%2F formats)
          SITEMAP_URL=$(python3 -c "import urllib.parse; print(urllib.parse.quote('https://subodhkc.com/sitemap.xml', safe=''))")

          # Try sc-domain format first
          RESPONSE=$(curl -s -w "\n%{http_code}" \
            -X PUT "https://www.googleapis.com/webmasters/v3/sites/sc-domain%3Asubodhkc.com/sitemaps/$SITEMAP_URL" \
            -H "Authorization: Bearer $ACCESS_TOKEN" \
            -H "Content-Type: application/json")
          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | head -n -1)
          echo "GSC sitemap submission (sc-domain): $HTTP_CODE"
          echo "Response: $BODY"

          if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "204" ]; then
            echo "Trying https:// format..."
            RESPONSE2=$(curl -s -w "\n%{http_code}" \
              -X PUT "https://www.googleapis.com/webmasters/v3/sites/https%3A%2F%2Fsubodhkc.com%2F/sitemaps/$SITEMAP_URL" \
              -H "Authorization: Bearer $ACCESS_TOKEN" \
              -H "Content-Type: application/json")
            HTTP_CODE2=$(echo "$RESPONSE2" | tail -1)
            BODY2=$(echo "$RESPONSE2" | head -n -1)
            echo "GSC sitemap submission (https): $HTTP_CODE2"
            echo "Response: $BODY2"
            if [ "$HTTP_CODE2" = "200" ] || [ "$HTTP_CODE2" = "204" ]; then
              echo "Sitemap submitted to Google Search Console"
            else
              echo "::warning::GSC sitemap submission failed with both formats (non-fatal)"
            fi
          else
            echo "Sitemap submitted to Google Search Console"
          fi

      - name: Verify IndexNow key is accessible
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://subodhkc.com/subodhkcindexnowkey2026.txt")
          echo "IndexNow key file status: $STATUS"

      - name: Verify RSS feed is accessible
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://subodhkc.com/feed.xml")
          echo "RSS feed status: $STATUS"

      - name: Verify sitemap is accessible
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://subodhkc.com/sitemap.xml")
          echo "Sitemap status: $STATUS"
